name: tasty_bytes_dbt_project
version: 1.0.0
config-version: 2
profile: tasty_bytes_dbt_project
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  tasty_bytes_dbt_project:
    +materialized: view
